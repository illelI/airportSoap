<Window x:Class="airportClient.CheckTicketWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:airportClient"
        mc:Ignorable="d"
        Title="CheckTicketWindow" Height="200" Width="400">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <TextBox x:Name="TicketInput"
                 Grid.Row="0"
                 Height="30"
                 Margin="0 0 0 10"
                />

        <Button Content="Sprawdź bilet"
                Grid.Row="1"
                Height="30"
                Width="120"
                HorizontalAlignment="Left"
                Click="CheckTicketButton_Click"/>
    </Grid>
</Window>
